# SQL Practice 03 — Aggregation, GROUP BY & HAVING

## Concepts
COUNT, COUNT(DISTINCT), SUM, AVG, MAX, MIN, GROUP BY, HAVING, WHERE before grouping.

## Critical pattern
`WHERE` filters rows before aggregation.
`HAVING` filters groups after aggregation.
